This is only first time things :3 :
git config --global user.name "AlbertSandager"
git config --global user.email "albert@sandbo.dk"



Get to folder:
cd /home/pi/P5-Platooning/

Pull everything from git:
git pull origin master

To add everything and push it:
git add .
git commit -m 'Write commit message here!'
git push origin master
